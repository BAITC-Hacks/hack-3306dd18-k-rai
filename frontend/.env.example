# Работать на моках (lib/mock.ts) или ходить в реальный API.
# Значение читается на этапе сборки — после изменения нужен npm run build.
NEXT_PUBLIC_USE_MOCKS=false

# База API. Пусто — тот же origin, что и статика.
NEXT_PUBLIC_API_URL=
